# 2026-05-12 Vue Router 学习 — 自我总结

> 掌握度：🟢真懂 / 🟡模棱两可 / 🔴没记住

---

## 一、我真正掌握的内容（🟢）

- hash 模式和 history 模式的区别：hash 带 # 不发服务器；history 需要服务器配置
- 路由匹配优先级：先注册先匹配，404 必须最后
- 导航守卫 beforeEach：Token 检查 → 拉用户信息 → 过滤动态路由 → addRoute
- 白名单机制：/login 和 /404 无需 Token

---

## 二、我没记住/不知道的内容（🔴）

- next({ path: "/" }) 和 next("/") 的区别：不知道
- replace: true 的作用：以为是确保成功，实际是替换历史记录不追加
- 路由守卫代码跟着 AI 写的，为什么用 { ...to, replace: true } 不知道

---

## 三、今天纠正的错误

- replace: true 不是确保成功，是不产生重复历史
- 跟着写过的代码不等于理解

---

## 四、明天的目标

1. 搞懂 next({ path }) vs next(string) 区别
2. 补完导航解析全流程
3. 开始 Pinia 源码